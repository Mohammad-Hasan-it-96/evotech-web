name: Deploy

# This repo had no CI at all, so the verify job below is doing double duty: it is
# both the pull-request gate and the thing that stops a broken build reaching the
# server.
#
# That second role matters more than usual here. deploy.sh removes .next before
# rebuilding, so a build that fails ON THE SERVER leaves the old process running
# with no build directory behind it — chunks 404 and the site breaks. Proving the
# build on identical inputs (same lockfile, npm ci) before we ever touch the VPS
# is what keeps that from happening.

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify:
    name: Typecheck · Lint · Build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      # ci, never install: install rewrites package-lock.json, which is how the
      # server ended up with a commit it had made to itself.
      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Lint
        run: npx eslint src --max-warnings=0

      - name: Build
        run: npm run build

  deploy:
    name: Deploy to production
    needs: verify
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    # Serialise: deploy.sh stops and restarts the app, so two overlapping runs
    # can leave the site down. A queued deploy beats a half-applied one.
    concurrency:
      group: deploy-web
      cancel-in-progress: false

    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          # deploy.sh runs 3-5 minutes (npm ci + build); the action's default is
          # shorter than that.
          command_timeout: 15m
          script_stop: true
          script: |
            set -Eeuo pipefail
            cd "${{ secrets.WEB_PATH }}"
            git pull --ff-only origin main
            ./deploy.sh

      # deploy.sh already health-checks locally on 127.0.0.1. This checks the
      # public URL, which also covers the proxy in front of it.
      #
      # WEB_BASE_URL is the origin with no trailing slash, e.g.
      # https://evotech-sys.com
      - name: Health check
        env:
          BASE: ${{ secrets.WEB_BASE_URL }}
        run: |
          code=$(curl -s -o /dev/null -w '%{http_code}' --max-time 20 "$BASE/ar")
          echo "site: $code"
          [ "$code" = "200" ] || { echo "Site is not healthy after deploy" >&2; exit 1; }

          # The response that tells the Fawateer app which API to talk to. A 404
          # makes the app silently fall back to its baked-in URL, so this failure
          # would otherwise look like nothing at all.
          #
          # The body is asserted, not just the status: this is now a route handler
          # that falls back to a committed default, so it answers 200 even when the
          # API is unreachable. Checking status alone could no longer fail.
          config=$(curl -s -w '\n%{http_code}' --max-time 15 "$BASE/config/fawateer.json")
          code=$(printf '%s' "$config" | tail -n1)
          body=$(printf '%s' "$config" | sed '$d')
          echo "config: $code"
          [ "$code" = "200" ] || { echo "/config/fawateer.json is not being served" >&2; exit 1; }
          printf '%s' "$body" | grep -q '"base_url"' \
            || { echo "/config/fawateer.json has no base_url — the app would use its baked-in URL" >&2; exit 1; }

          # Serving the fallback is a warning, not a failure: the app still gets a
          # working config. Failing here would block an unrelated web deploy on the
          # API being briefly down.
          source=$(curl -s -o /dev/null -D - --max-time 15 "$BASE/config/fawateer.json" \
            | tr -d '\r' | awk -F': ' 'tolower($1)=="x-config-source"{print $2}')
          [ "$source" = "fallback" ] \
            && echo "::warning::/config/fawateer.json is serving the built-in fallback — dashboard edits are not live." \
            || echo "config source: ${source:-unknown}"
